import React from "react";

interface ResultHeaderProps {
  resultCount: number;
}

export const ResultHeader: React.FC<ResultHeaderProps> = ({ resultCount }) => {
  return (
  
  );
};

export default ResultHeader;